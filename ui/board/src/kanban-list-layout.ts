export const KANBAN_LIST_RAIL_CLASS_NAME = "mx-auto w-full max-w-2xl"

/** Wide, left-aligned variant of the list rail: fills the full pane width with
 *  no max cap, so cards span nearly edge to edge (the list container's padding
 *  is the only inset). Opt-in via the rail's `align` prop and used by the
 *  Archived views — a wide list lets the chat panel open beside it while the
 *  missions stay visible, the cards shrinking with the pane as it narrows. */
export const KANBAN_LIST_RAIL_LEFT_CLASS_NAME = "w-full"
